# 第 1 批 PoC 结果报告

日期：2026-09-23
环境：macOS 26.6.2 (25G83) / Apple Silicon / Swift 6.3.3（仅 Command Line Tools）
测试图：1280×720 合成画面（深色背景 + 网址 + 代码 + 日文字幕条），模拟教程类视频

---

## 一句话结论

**核心场景成立，并且已在真实视频截图上验证过。**

- 真实截图（B 站视频，1920×1231，带压缩+水印）：中文**满分**——`李勃老师`、`那你试着把这个程序给停掉` 均 conf=1.00；密集终端文字 128 行里 **72% 满分**；坐标框贴合准确
- 单次耗时：合成小图 ~400ms，真实 1920 图 **560–1130ms**（取决于语言包，见第十节）
- **最重要的发现：中日语言包互斥。** 同时给出会让中文被"日化"成繁体/日文汉字变体（师→姉、试→試、这→汶），且耗时翻近一倍。**默认已改为 `zh-Hans,en-US`**
- 两个已确认的坑：**`fast` 模式不可用**（中日文崩坏）；**低对比度文字**识别差（但 conf 会掉到 0.30，可用于过滤）

---

## 一、PoC 五项验证

| # | 验证项 | 结果 | 数据 |
|---|---|---|---|
| 1 | CLT-only 能否编译链接 Vision | ✅ **通过** | Swift 6.3.3 编译成功，二进制 141K，无需完整 Xcode |
| 2 | OCR 质量 | ✅ **核心场景通过** | 详见第二节 |
| 3 | 坐标精度 | ✅ **通过** | 框紧贴文字，见 `boxes.png` |
| 4 | 单次耗时 | ✅ **可接受** | accurate 稳定 **~400ms**；fast ~125ms |
| 5 | 中日英混排 | ✅ **通过** | 三语同时指定，日文完美，互不干扰 |

---

## 二、识别质量明细

| 内容 | 原文 | 识别结果 | conf | 判定 |
|---|---|---|---|---|
| 网址 | `https://github.com/apple/swift` | 完全一致 | **1.00** | ✅ |
| 代码 | `swift build -c release` | 完全一致 | **1.00** | ✅ |
| 日文字幕 | `これはテストです。字幕の認識精度を確認します。` | 完全一致 | **1.00** | ✅ |
| 角标 | `Tutorial 03 / 12` | 完全一致 | **1.00** | ✅ |
| 命令 | `./livetext-ocr sample.png` | `'/Livetext-ocr sample.png` | 1.00 | ❌ |
| 灰色注释 | `// 输出 JSON，含每行文字的归一化坐标` | `11輸出 JSON，含每行文字的ー化` | **0.30** | ❌ |
| 角标 | `sample frame 001` | `sample frame` | 1.00 | ❌ |

### 规律很清楚

- **高对比 + 字号够大 → 完美。** 网址、代码、日文全部 `conf = 1.00`，一字不差。
- **低对比灰字 → 崩。** conf 掉到 `0.30`，**Vision 自己知道这行不确定**——这是好消息，说明可以拿 conf 做过滤或提示。
- **等宽小字易混。** `l` → `L`、`.` → `'`，这是模型层面的局限，无解。
- **尾部字符可能丢。** `sample frame 001` → `sample frame`。

---

## 三、压缩容忍度（模拟视频编码）

把测试图转成 **JPEG 质量 30%**（34KB，压缩率约 90%）再跑：

| 内容 | 压缩后表现 |
|---|---|
| 网址 | ✅ **依然完全正确，conf 仍为 1.00** |
| 代码 | ✅ 依然完全正确 |
| 日文字幕 | ⚠️ 日文主体正确，但与相邻的 `sample frame` **被合并成一行** |
| 灰色注释 | ❌ 更差 |

**结论：JPEG 压到 30% 不致命。** 真实视频的编码压缩应该扛得住——这条很重要。

---

## 四、重要结论：fast 模式不能用

| | accurate | fast |
|---|---|---|
| 耗时 | ~400ms | ~125ms（快 3.2 倍） |
| 网址 | ✅ | ✅ |
| `swift build -c release` | ✅ | ✅ |
| 命令行 | 部分错 | `. /livetext-oc r sample. png`（空格乱插） |
| 中文 | 差（conf 0.30） | **完全崩坏**：`// JSON, É*ts*+ÉIiJa-It￿l1T` |
| 日文 | ✅ | **整行消失**（根本没识别出来） |
| 置信度 | **有区分度**（1.00 / 0.30） | **恒为 0.50，无参考价值** |

> **决策：默认 accurate，弃用 fast。**
> 400ms 对「用户点完小标后等待」完全可以接受——这符合心理预期。
> 而 fast 会直接毁掉两个核心场景（日文、中文），还会让置信度失去意义。

---

## 五、已实现的工具能力

```
livetext-ocr <图片> [--langs=ja-JP,zh-Hans,en-US] [--min-conf=0.5] [--fast]
    识别图片，JSON 结果打到 stdout（含每行文字、归一化坐标、置信度）

livetext-ocr draw <图片> <json> [-o <输出图>]
    把识别框画回图上：红框 = 高置信度，琥珀框 = 存疑（conf < 0.5）

livetext-ocr langs
    列出本机 Vision 支持的 30 种语言
```

坐标约定：输出 Vision 的归一化坐标，**原点在左下，与 AppKit / CGContext 一致，无需翻转**——这一点直接对应后面的文本层渲染。

---

## 六、已知弱项与缓解

| 弱项 | 影响 | 缓解手段 |
|---|---|---|
| 低对比度文字 | 识别崩坏，conf 0.30 | `--min-conf` 过滤，或界面标黄提示 |
| 等宽小写 `l` / 点号 | `l`→`L`、`.`→`'` | 无解，属模型局限 |
| 相邻行被合并 | 压缩后字幕与邻近文字串成一行 | 按 y 坐标做行切分后处理（第 3 批做） |
| 尾部字符丢失 | `001` 丢失 | 结合 conf + 长度异常提示 |

---

## 七、产物清单

```
livetext/
├── host/
│   ├── livetext-ocr.swift        OCR 主体（OCR / draw / langs 三模式）
│   ├── livetext-ocr              编译产物（141K，可直接运行）
│   └── build.sh                  一键编译
└── poc/
    ├── test.html                 测试图源（模拟视频画面）
    ├── test_frame.png            1280×720 测试图
    └── out/
        ├── result.json                 识别结果（坐标 + 置信度）
        ├── boxes.png                   框贴合图 ← 看这张
        ├── compressed.jpg              JPEG q30 压缩版
        ├── result_compressed.json      压缩版识别结果
        └── boxes_compressed.png        压缩版框图
```

---

## 八、下一步：需要一张你的真实截图

**现在这张测试图是我用 HTML 合成的，它比真实视频干净得多。** 合成图无法复现：

- 真实视频编码的块效应（尤其在暗场）
- 硬字幕的半透明描边
- 复杂 / 杂乱的背景
- 720p / 480p 下更小的字号
- 动态模糊

所以**第 2 项 PoC 必须在真实截图上做**。

> **给我一张你实际要看的视频截图**（暂停状态，画面里带你想抓的那种文字——网址、代码、或者日文字幕），我跑一遍，就能确定这条路对你的真实场景成不成立。

在那之前，**不用装任何东西**，也不用动 Chrome。

---

# 真实视频截图验证（17:25 追加）

卓提供了两张真实 B 站截图（均 1920×1231，带视频编码压缩 + 站方水印）。

## 九、两张真实截图的表现

| | 低难度（GitHub issue 页面） | 高难度（VS Code + 终端） |
|---|---|---|
| 识别行数 | 28 行 | **128–129 行** |
| **conf = 1.00 满分** | **26 行（93%）** | **93 行（72%）** |
| conf 0.5–0.99 | 2 行 | 32 行 |
| conf < 0.5 存疑 | **0 行** | 4 行 |
| 耗时（`zh-Hans,en-US`） | **566 ms** | **1123 ms** |
| 耗时（三语，未优化） | 1005 ms | 1978 ms |

**两张真实截图都扛住了。** 坐标框贴合准确，连 VS Code 侧边栏里被视频本身截断的文字（`zero_to_te...`）、macOS 菜单栏、底部 Dock 栏都被识别出来。低难度那张 **93% 满分、零存疑**。

## 十、重大发现：中日语言包互斥

### 现象

同一张中文截图，只改语言参数，结果完全不同：

| 语言参数 | 中文识别结果 | 耗时 |
|---|---|---|
| `ja-JP,zh-Hans,en-US` | ❌ `李勃老姉` / `那你可以試着把汶个程序給停掉`（conf **0.30**） | 1978 ms |
| `ja-JP,en-US` | ❌ 同样错 | 1929 ms |
| **`zh-Hans,en-US`** | ✅ **`李勃老师` / `那你可以试着把这个程序给停掉`（conf 1.00）** | **1051 ms** |
| `zh-Hans` | ✅ 同上（conf 1.00） | 984 ms |

**一旦掺入 `ja-JP`，简体字就被识别成日文汉字或繁体变体：**

| 原文 | 被识别成 |
|---|---|
| 师 | 姉（**日文汉字**） |
| 试 | 試（繁体） |
| 给 | 給（繁体） |
| 这 | 汶（错字） |

### 反向验证

拿带日文字幕的图，反过来测：

| 语言参数 | 日文识别结果 |
|---|---|
| `ja-JP,en-US` | ✅ `これはテストです。字幕の認識精度を確認します。`（conf 1.00） |
| `ja-JP,zh-Hans,en-US` | ✅ 同上（conf 1.00） |
| **`zh-Hans,en-US`** | ❌ **日文 0 行**——假名完全认不出 |

### 结论

> **中日语言包互斥，不能靠"两个都给"来蒙。**
> - 给 `ja-JP` → 日文完美，**中文被日化**
> - 给 `zh-Hans` → 中文完美，**日文完全消失**
> - 给两个 → **日文赢**，中文仍然受损，而且慢近一倍

**这直接决定扩展的设计**：必须提供**语言切换**，不能写死一套语言。
默认 `zh-Hans,en-US`（覆盖 B 站 / YouTube 中文 + 英文技术内容，且快近一倍），看日文内容时切到 `ja-JP,en-US`。

### 性能收益

| 场景 | 三语 | 精简为 `zh-Hans,en-US` | 提升 |
|---|---|---|---|
| 低难度真实图 | 1005 ms | **566 ms** | **−44%** |
| 高难度真实图 | 1978 ms | **1123 ms** | **−43%** |
| 合成小图 1280×720 | 440 ms | 410 ms | 基本无差（图小、字少） |

### 追加产物

```
poc/
├── real_hard.jpg                高难度原图（VS Code + 终端）
├── real_easy.jpg                低难度原图（GitHub issue）
└── out/
    ├── real_hard.json           高难度识别结果
    ├── real_hard_boxes.png      高难度框图 ← 中文框已由琥珀变红
    ├── real_easy.json           低难度识别结果
    └── real_easy_boxes.png      低难度框图 ← 93% 满分、零存疑
```
